/**
 在c中struct 只单纯的用作数据的复合类型，也就是说，在结构体声明中只能将数据成员放在里面，
 而不能将函数放在里面。

 在结构体声明中不能使用c++的访问修饰符，如：public protected private

 在c中定义的结构体变量，如果使用了下面定义必须加struct

 c的结构体不能继承

 如果结构体名字和函数名字一样，可以正常运行

**/

/*

 c++ 对比 c
 c++中结构体不仅可以定义数据，还可以定义函数

 c++中结构体可以使用修饰符 public protected private

 c++继承

 总体来说 struct 更适合看成一个数据结构的实现体，class是一个对象实体

 本质区别是默认访问控制
 struct public

 class private


*/