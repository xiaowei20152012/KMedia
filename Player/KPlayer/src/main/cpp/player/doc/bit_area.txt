/**
bit field

位域 或 位段 为一种数据结构， 可以把数据以位的形式紧凑存储，并且允许程序员对此结构的位进行操作
这种数据结构的一个好处是它可以使数据单元节省存储空间，当程序需要成千上万个数据单元时，这种方法
就显得更为重要。第二个好处是位段可以更方便的访问一个整数值部分内容可以简化程序源代码。而这种
数据结构的缺点在于，位段实现依赖于具体的机器系统，不同的机器不同的结果，位段不可移植

位域 在内存中的布局与机器有关

位域 的类型必须是整型或者枚举，带符号类型中的位域的行为将因具体的实现而定

取址运算符 & 不能作用位域， 任何指针都无法指向类的位域

**/